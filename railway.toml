[build]
builder = "NIXPACKS"
nixpacks = { providers = ["python", "node"] }
buildCommand = "pip install -r requirements.txt && cd telegram_ui && npm install"
startCommand = "bash start.sh"

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

